# Verify-FileNameAndPathLimits
PowerShell script to scan directories and list files with names or paths exceeding strict length limits in characters and UTF-8 bytes, ensuring compatibility with Windows, exFAT, and Linux file systems.
